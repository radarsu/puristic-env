# Local overrides. DATABASE_URL is stored as plaintext here on purpose — the manager flags it
# as a plaintext secret (⚠) and "Encrypt all plaintext secrets" encrypts it across files.
NODE_ENV=development
SERVER_PORT=3000
DATABASE_URL=postgres://user:pass@localhost:5432/dev
LEGACY_FLAG=1
